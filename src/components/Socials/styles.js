const container = {
    display: 'flex',
    flexDirection: 'column'
}

const socialButton = {
    justifyContent: 'left'
}

const buttonStyle = {
    textTransform: 'none',
}

const iconStyle = {
    marginRight: '10px'
}

const iconStyleMobile = {
    paddingLeft: '0px'
}

module.exports = {
    container,
    socialButton,
    buttonStyle,
    iconStyle,
    iconStyleMobile
}