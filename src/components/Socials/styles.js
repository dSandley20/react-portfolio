const container = {
    display: 'flex',
    flexDirection: 'column'
}

const socialButton = {
    justifyContent: 'left'
}

const buttonStyle = {
    textTransform: 'none',
}

const iconStyle = {
    marginRight: '10px'
}

module.exports = {
    container,
    socialButton,
    buttonStyle,
    iconStyle
}